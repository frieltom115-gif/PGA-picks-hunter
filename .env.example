# A licensed odds provider or data-export job should write normalized JSON to this path.
ODDS_INPUT_PATH=data/provider-odds.json

# Required only if a future provider adapter makes HTTP calls. Never commit the value.
ODDS_PROVIDER_API_KEY=
